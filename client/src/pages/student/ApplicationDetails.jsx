import {useEffect,useState} from "react";import {useParams,Link} from "react-router-dom";import axios from "axios";
export default function ApplicationDetails(){const{id}=useParams();const[a,setA]=useState(null);useEffect(()=>{axios.get(`/api/applications/${id}`,{headers:{Authorization:`Bearer ${localStorage.getItem("token")}`}}).then(r=>setA(r.data.application||r.data)).catch(console.error)},[id]);if(!a)return <main className="page section">Loading...</main>;return <main className="page section"><Link to="/student/applications">← Applications</Link><div className="card detail"><h1>{a.job?.title||"Application"}</h1><p>{a.job?.company?.name||"-"}</p><p>Status: <span className="badge">{a.status}</span></p><p>Applied: {a.createdAt?new Date(a.createdAt).toLocaleDateString():"-"}</p></div></main>}
