import {useEffect,useState} from "react";import {Link} from "react-router-dom";import axios from "axios";
export default function MyJobs(){const[j,setJ]=useState([]);const load=()=>axios.get("/api/jobs/recruiter/my",{headers:{Authorization:`Bearer ${localStorage.getItem("token")}`}}).then(r=>setJ(r.data.jobs||r.data||[])).catch(console.error);useEffect(load,[]);const del=async id=>{if(!confirm("Delete this job?"))return;await axios.delete(`/api/jobs/${id}`,{headers:{Authorization:`Bearer ${localStorage.getItem("token")}`}});load()};return <main className="page section"><div className="actions"><h1>My Jobs</h1><Link className="btn primary" to="/recruiter/jobs/create">Create Job</Link></div><div className="card"><table><thead><tr><th>Job</th><th>Type</th><th>Actions</th></tr></thead><tbody>{j.map(x=><tr key={x._id}><td>{x.title}</td><td>{x.jobType}</td><td><Link to={`/recruiter/jobs/edit/${x._id}`}>Edit</Link> · <Link to={`/recruiter/jobs/${x._id}/applicants`}>Applicants</Link> · <button className="link-btn" onClick={()=>del(x._id)}>Delete</button></td></tr>)}</tbody></table></div></main>}
